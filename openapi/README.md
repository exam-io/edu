# OpenAPI Scaffold

This folder contains the API contract baseline for EduOS Step 1.

- Source of truth: `openapi/openapi.yaml`
- Public endpoints:
  - `GET /api/v1/openapi`
  - `GET /api/v1/openapi.yaml`

The document currently includes platform foundation endpoints only.
Domain APIs will be expanded module by module in later steps.
